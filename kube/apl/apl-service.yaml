# Create a service
kind: Service
apiVersion: v1
metadata:
  name: apl
  annotations:
    eks.amazonaws.com/role-arn: "arn:aws:iam::813173347981:role/eks-eks-nodes"
spec:
  selector:
    app: apl
  ports:
    - name: apl
      targetPort: 9092
      port: 9092
  type: LoadBalancer
